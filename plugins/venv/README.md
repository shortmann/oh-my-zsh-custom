# venv

The plugin activates a virtualenv when a .venv folder inside the project exists. It shows up a 🐍 when the environment is activated.

To use it, add `venv` to the plugins array of your zshrc file:
```
plugins=(... venv)
```
